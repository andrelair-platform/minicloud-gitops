apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jitsi
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: minicloud-platform
  sources:
    - repoURL: https://jitsi-contrib.github.io/jitsi-helm/
      chart: jitsi-meet
      targetRevision: 2.21.0
      helm:
        valueFiles:
          - $values/helm-values/minicloud-1/jitsi-values.yaml
    - repoURL: https://github.com/andrelair-platform/minicloud-gitops.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/andrelair-platform/minicloud-gitops.git
      targetRevision: main
      path: manifests/jitsi
  destination:
    server: https://kubernetes.default.svc
    namespace: collab
  ignoreDifferences:
    # priorityClassName set via kubectl patch — chart doesn't template this field
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.spec.priorityClassName
    - group: apps
      kind: StatefulSet
      jqPathExpressions:
        - .spec.template.spec.priorityClassName
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=false
