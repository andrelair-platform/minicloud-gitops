auditInterval: 60
constraintViolationsLimit: 100
replicas: 1
# No node pins: gatekeeper controller-manager + audit are stateless — unpinned so they
# reschedule to any healthy node if theirs goes down (auto-failover; SPOF hardening).
controllerManager: {}
audit: {}
