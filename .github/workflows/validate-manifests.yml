name: Validate Kubernetes Manifests

on:
  pull_request:
    branches: [main]
    paths:
      - 'services/**'
      - 'manifests/**'

env:
  KUBECONFORM_VERSION: "0.6.7"
  K8S_VERSION: "1.36.0"

jobs:
  kubeconform:
    name: kubeconform
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install kustomize
        uses: imranismail/setup-kustomize@v3
        with:
          kustomize-version: "5.6.0"

      - name: Install kubeconform
        run: |
          curl -sL "https://github.com/yannh/kubeconform/releases/download/v${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz" \
            | tar xz kubeconform
          sudo mv kubeconform /usr/local/bin/

      # Validate every Kustomize overlay. Custom resources (Rollout, ScaledObject,
      # ExternalSecret, Certificate, ServiceMonitor, etc.) are skipped with
      # -ignore-missing-schemas — only core k8s objects are validated strictly.
      - name: Validate service overlays
        run: |
          set -euo pipefail
          FAILED=0
          for overlay in services/*/minicloud-1/*/; do
            echo "── $overlay"
            if ! kustomize build "$overlay" \
              | kubeconform \
                  -strict \
                  -kubernetes-version "$K8S_VERSION" \
                  -ignore-missing-schemas \
                  -summary \
                  -; then
              echo "FAIL: $overlay"
              FAILED=1
            fi
          done
          exit $FAILED

      # Validate raw manifests/ YAML files. kustomization.yaml files and CRD
      # definition files are excluded (kubeconform validates instances, not schemas).
      - name: Validate manifests/
        run: |
          set -euo pipefail
          find manifests/ -name "*.yaml" \
            ! -name "kustomization.yaml" \
            ! -path "*/gatekeeper-policies/*" \
            | sort \
            | kubeconform \
                -strict \
                -kubernetes-version "$K8S_VERSION" \
                -ignore-missing-schemas \
                -summary \
                -
