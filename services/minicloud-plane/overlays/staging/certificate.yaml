apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minicloud-plane-staging-tls
spec:
  secretName: minicloud-plane-staging-tls
  issuerRef:
    name: minicloud-ca
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - minicloud-plane-staging.10.0.0.200.nip.io
  duration: 2160h
  renewBefore: 720h
  privateKey:
    algorithm: ECDSA
    size: 256
