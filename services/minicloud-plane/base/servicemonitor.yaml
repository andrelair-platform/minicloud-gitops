apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minicloud-plane
spec:
  selector:
    matchLabels:
      app: minicloud-plane
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
