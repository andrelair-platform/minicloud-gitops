apiVersion: v1
kind: Service
metadata:
  name: minicloud-plane
  labels:
    app: minicloud-plane
spec:
  selector:
    app: minicloud-plane
  ports:
    - name: http
      port: 8080
      targetPort: 8080
