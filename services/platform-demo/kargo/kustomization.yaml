apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Kargo promotion pipeline for platform-demo (see manifests/kargo/README.md).
resources:
  - namespace.yaml
  - project.yaml
  - git-credentials.yaml
  - image-credentials.yaml
  - warehouse.yaml
  - stage-dev.yaml
  - stage-prod.yaml
