# dev overlay — Harbor image, 1 replica, dev host, KEDA scale-to-zero (HTTP add-on driven)
image:
  repository: harbor.10.0.0.200.nip.io/library/platform-demo
  tag: dev-latest          # Kargo bumps this
workload:
  replicas: 1
ingress:
  enabled: true
  host: platform-demo-dev.10.0.0.200.nip.io
  servicePort: 8080
