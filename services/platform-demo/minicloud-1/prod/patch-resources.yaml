  # replicas removed — KEDA platform-demo-cron-scaler owns it (scale-to-zero when idle).
- op: replace
  path: /spec/template/spec/containers/0/resources
  value:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
