apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gitops-demo
resources:
- ../../base
- ingress.yaml
- certificate.yaml
# Tag promoted manually: after staging is validated, open a PR bumping newTag here.
# ArgoCD syncPolicy.automated is disabled for prod — requires manual sync click.
images:
- name: harbor.10.0.0.200.nip.io/library/platform-demo
  newTag: 2c09e65
patches:
- path: patch-resources.yaml
