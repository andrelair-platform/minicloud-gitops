apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: platform-demo
spec:
  selector:
    matchLabels:
      app: platform-demo
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
