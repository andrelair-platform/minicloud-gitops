apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: backend-prod
  namespace: retrieva-kargo
spec:
  # Prod: only backend Freight already verified in backend-dev is promotable (the dev->prod
  # gate). Bumps only backend.image.tag in values-prod.yaml (ghcr SHA). PR is CODEOWNERS-gated;
  # ArgoCD auto-syncs prod once merged (squash — signed-commit rule).
  requestedFreight:
    - origin: { kind: Warehouse, name: retrieva-backend }
      sources: { stages: [backend-dev] }
  promotionTemplate:
    spec:
      vars:
        - name: gitopsRepo
          value: https://github.com/andrelair-platform/minicloud-gitops.git
        - name: img
          value: ghcr.io/andrelair-platform/retrieva-backend
      steps:
        - uses: git-clone
          as: clone
          config:
            repoURL: ${{ vars.gitopsRepo }}
            checkout:
              - branch: main
                path: ./repo
        - uses: yaml-update
          as: set-image
          config:
            path: ./repo/services/retrieva/helm/values-prod.yaml
            updates:
              - key: backend.image.tag
                value: '${{ quote(imageFrom(vars.img).Tag) }}'
        - uses: git-commit
          as: commit
          config:
            path: ./repo
            message: 'kargo(retrieva): promote backend ${{ imageFrom(vars.img).Tag }} to prod'
        - uses: git-push
          as: push
          if: ${{ outputs.commit.commit != outputs.clone.commits['./repo'] }}
          config: { path: ./repo, generateTargetBranch: true }
        - uses: git-open-pr
          as: open-pr
          if: ${{ outputs.commit.commit != outputs.clone.commits['./repo'] }}
          config:
            repoURL: ${{ vars.gitopsRepo }}
            sourceBranch: ${{ outputs.push.branch }}
            targetBranch: main
            provider: github
            title: 'kargo(retrieva): promote backend ${{ imageFrom(vars.img).Tag }} to PROD'
            description: 'Promotion opened by Kargo — retrieva BACKEND @ ${{ imageFrom(vars.img).Tag }} to prod (retrieva.online). CODEOWNERS-gated; ArgoCD auto-syncs prod once merged.'
            labels: [kargo, env-prod]
        - uses: git-wait-for-pr
          as: wait-pr
          if: ${{ outputs.commit.commit != outputs.clone.commits['./repo'] }}
          config:
            repoURL: ${{ vars.gitopsRepo }}
            prNumber: ${{ outputs['open-pr'].pr.id }}
