apiVersion: v2
name: kargo-ktayl-iam
description: >-
  Kargo promotion pipeline for ktayl-iam. Raw Kargo CRs under files/ emitted verbatim by
  templates/manifests.yaml via .Files.Get (NOT Helm-templated, so Kargo/ESO ${{ }} + {{ }}
  expressions are preserved). Packaging shim replacing the former kustomize resources: list.
type: application
version: 0.1.0
