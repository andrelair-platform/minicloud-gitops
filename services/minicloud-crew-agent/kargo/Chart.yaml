apiVersion: v2
name: kargo-minicloud-crew-agent
description: >-
  Kargo promotion pipeline for minicloud-crew-agent. Raw Kargo CRs under files/ emitted verbatim by
  templates/manifests.yaml via .Files.Get (NOT Helm-templated, so Kargo/ESO ${{ }} + {{ }}
  expressions are preserved). Packaging shim replacing the former kustomize resources: list.
type: application
version: 0.1.0
