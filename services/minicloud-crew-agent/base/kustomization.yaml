apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - poddisruptionbudget.yaml
# images: block intentionally absent — each overlay pins its own tag.
