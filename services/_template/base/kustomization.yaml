apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
# No images: block here — each overlay pins its own tag.
# No namespace: here — each overlay sets its own namespace.
