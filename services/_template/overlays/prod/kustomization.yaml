apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: SERVICE_NAME-prod
resources:
  - ../../base
  # Add ingress.yaml and certificate.yaml here for prod.
# Promoted manually: PR bumping newTag after staging is approved.
# ArgoCD Application for prod should have syncPolicy.automated disabled.
images:
  - name: harbor.10.0.0.200.nip.io/library/SERVICE_NAME
    newTag: "latest"
patches:
  - path: patch-resources.yaml
