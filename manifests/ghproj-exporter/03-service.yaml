apiVersion: v1
kind: Service
metadata:
  name: ghproj-exporter
  namespace: monitoring
  labels:
    app: ghproj-exporter
spec:
  selector:
    app: ghproj-exporter
  ports:
    - name: http-metrics
      port: 8080
      targetPort: http-metrics
