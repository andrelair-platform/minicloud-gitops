apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: cluster-pool
  namespace: kube-system
spec:
  # Mirrors MetalLB cluster-pool: 10.0.0.200-10.0.0.250
  blocks:
    - start: "10.0.0.200"
      stop: "10.0.0.250"
