apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-ui-tls
  namespace: kube-system
spec:
  secretName: hubble-ui-tls
  issuerRef:
    name: minicloud-ca
    kind: ClusterIssuer
  dnsNames:
    - hubble.10.0.0.200.nip.io
